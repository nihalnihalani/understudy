#!/usr/bin/env bash
# Pre-provision InsForge 2.0 warm-pool backends for the demo (architecture.md §18 risk #3).
# TODO(task #5): call InsForge admin API to create 3 tenants, register Remote OAuth MCP
# endpoints, and seed PostgREST tables from the ER diagram in architecture.md §8.
set -euo pipefail

echo "TODO(task #5): provision InsForge warm pool"
exit 1

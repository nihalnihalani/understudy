// TinyFish CLI agent entrypoint — generated agents replace the body of `run()`.
// See architecture.md §5 (Generated Agent Runtime).

export async function run(): Promise<void> {
  // TODO(task #5): connect Agent Memory Server, hydrate pinned skills, serve GraphQL.
  throw new Error("see task #5");
}

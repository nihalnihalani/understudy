# Generated by Cosmo Dream Query for agent `export-shopify-orders`.
# The original user request: "export yesterday's Shopify orders as CSV".
# Dream Query proposed this SDL delta and validated against live client traffic.

type Query {
  exportOrders(dateRange: String = "yesterday", format: ExportFormat = CSV): ExportResult!
  recentExports(limit: Int = 10): [ExportResult!]!
}

type Mutation {
  triggerExport(dateRange: String!, format: ExportFormat = CSV): ExportResult!
}

enum ExportFormat {
  CSV
  JSON
  PARQUET
}

type ExportResult {
  id: ID!
  rowCount: Int!
  downloadUrl: String!
  format: ExportFormat!
  generatedAt: String!
}

"""Router config loader — the router itself is the upstream Cosmo binary; this module manages its config."""


def load_router_config() -> dict[str, object]:
    # TODO(task #6): render config.yaml from registered subgraphs + EDFS bindings.
    raise NotImplementedError("see task #6")
